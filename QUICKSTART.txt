╔═══════════════════════════════════════════════════════════════════╗
║                 KUBERNETES CLUSTER QUICK START                    ║
║                    (Linux + VirtualBox)                           ║
╚═══════════════════════════════════════════════════════════════════╝

📍 YOU ARE HERE: Ready to test the cluster setup
🎯 GOAL: Get a 3-node Kubernetes 1.34.1 cluster running

┌───────────────────────────────────────────────────────────────────┐
│ STEP 1: Switch to VirtualBox (Required!)                         │
└───────────────────────────────────────────────────────────────────┘

In a REAL terminal (with sudo access):

    cd /home/jpoley/vagrant-linux
    ./switch-to-virtualbox.sh

Expected: Module switching completes, VirtualBox ready
If asked about vboxusers: LOG OUT & LOG BACK IN, then re-run

┌───────────────────────────────────────────────────────────────────┐
│ STEP 2: Start the Cluster                                        │
└───────────────────────────────────────────────────────────────────┘

    vagrant up

Expected: 20-30 minutes, downloads box and provisions 3 nodes
Sit back, relax, watch the automation magic happen ☕

┌───────────────────────────────────────────────────────────────────┐
│ STEP 3: Verify Everything Works                                  │
└───────────────────────────────────────────────────────────────────┘

    ./verify-cluster.sh

Expected: All 3 nodes show "Ready" status

┌───────────────────────────────────────────────────────────────────┐
│ STEP 4: Play with Kubernetes!                                    │
└───────────────────────────────────────────────────────────────────┘

    vagrant ssh k8s-cp
    kubectl get nodes
    kubectl create deployment nginx --image=nginx
    kubectl get pods

═══════════════════════════════════════════════════════════════════

🆘 IF SOMETHING BREAKS

1. Copy the EXACT error message
2. Run: lsmod | grep -E "(kvm|vbox)"
3. Run: vagrant status
4. Check TESTING-GUIDE.md for detailed debugging
5. Report back with details

═══════════════════════════════════════════════════════════════════

🔄 SWITCHING BACK TO KVM (for Slicer)

When done with Kubernetes:

    vagrant halt
    ./switch-to-kvm.sh

To return to Kubernetes:

    ./switch-to-virtualbox.sh
    vagrant up

═══════════════════════════════════════════════════════════════════

📖 MORE INFO

README.md         - Full documentation
TESTING-GUIDE.md  - Detailed testing instructions
CLAUDE.md         - Technical architecture

═══════════════════════════════════════════════════════════════════

Ready? Let's do this! 🚀

    ./switch-to-virtualbox.sh

═══════════════════════════════════════════════════════════════════
